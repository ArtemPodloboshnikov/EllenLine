import classes from './List.module.scss';

const List = (props) => {
    return (
        <form className={props.className + ' ' + classes.form}>
            dsds
        </form>
    )
}

export default List
